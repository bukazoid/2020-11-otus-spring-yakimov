spring:
  datasource:
    url: jdbc:h2:mem:library;MODE=postgresql
    initialization-mode: always
    
  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none
    show-sql: true
    
  h2:
    console:
      enabled: false 
    
  liquibase:
    enabled: true
    